const User = require("../models/studytime")
const router = require("express").Router();

router.post('/', )
let num = 0;
    let timer = setInterval(function(){
            console.log(num++);
        }, 1000)